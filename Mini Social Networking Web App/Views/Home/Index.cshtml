@model IEnumerable<Mini_Social_Networking_Web_App.Models.Gig>

@{
    ViewBag.Title = "Home Page";
}

<ul class="gigs">
    @foreach (var gig in Model)
    {
        <li>
            <div class="date">
                <div class="month">
                    @gig.DateTime.ToString("MMM")
                </div>
                <div class="day">
                    @gig.DateTime.ToString("d ")
                </div>
            </div>
            <div class="details">
                <span class="artist">
                    @gig.Artist.Name
                </span>
                <span class="genre">
                    @gig.Genre.Name
                </span>
                <button data-artist-id="@gig.ArtistId" class="btn btn-link js-toggle-following">Follow</button>
                <button data-gig-id="@gig.Id" class="btn btn-sm pull-right btn-default js-toggle-attendance">Going ?</button>
            </div>
        </li>
    }
</ul>

@section scripts{
    <script>
        $(document).ready(function () {
            $(".js-toggle-attendance").click( function(e) {
                var button = $(e.target);
                $.post("/api/attendances", { GigId: button.attr("data-gig-id") })
                .done(function () {
                    button
                        .removeClass("btn-default")
                        .addClass("btn-info")
                        .text("Going");
                })
                .fail(function () {
                    alert("Something failed!!");
                });
            });

            $(".js-toggle-following").click(function (e) {
                var button = $(e.target);
                $.post("/api/following", { ArtistId: button.attr("data-artist-id") })
                .done(function () {
                    button
                        .text("Following");
                })
                .fail(function () {
                    alert("Something failed!!");
                });
            });

        });
    </script>
}